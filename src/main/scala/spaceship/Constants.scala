package spaceship

object Constants {
  val maxBlackHoles = 1
  val maxAsteroids = 10
  def screenCenter = Vec2(Universe.game.width / 2, Universe.game.height / 2)
  // val maxBullets = ???
  val maxVelocity = Vec2(50, 50)
  // val velocityCap = 150

}
